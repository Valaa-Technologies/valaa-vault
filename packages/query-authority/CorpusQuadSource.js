/**
 * Implements RDFJS source interface by wrapping Valker object
 * and converts the source interface match calls to VALK calls
 *
 * @export
 * @class CorpusQuadSource
 */
export default class CorpusQuadSource {
	constructor() {
		
	}
}
